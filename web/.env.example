# Base path for the REST API. Leave as-is to use the Vite dev proxy (same origin).
VITE_API_BASE_URL=/api/v1

# Origin used to resolve relative /uploads/... image URLs from the API.
# Leave empty to resolve against the current origin (recommended with the proxy).
VITE_ASSET_BASE_URL=
